# Railway uchun Nixpacks konfiguratsiyasi (Backend uchun)
# Backend uchun faqat dependencies o'rnatish va server ishga tushirish kerak
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

# Build phase ni o'chirish - backend uchun kerak emas
# [phases.build] - o'chirilgan

[start]
cmd = "npm start"

