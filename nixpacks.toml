# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "rm -f package-lock.json || true",
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = []

[start]
cmd = "npm start"

