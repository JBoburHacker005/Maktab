# Nixpacks configuration for Railway
# Node.js version will be detected from .nvmrc or .node-version
[phases.setup]
nixPkgs = []

[phases.install]
cmds = [
  "rm -f package-lock.json || true",
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = []

[start]
cmd = "npm start"

